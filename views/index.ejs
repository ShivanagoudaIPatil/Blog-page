<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>PESU Post</title>
    <link rel="stylesheet" href="/style.css">
    <!-- <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"> -->
    <!-- <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200&icon_names=thumb_up" /> -->
    <!-- <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined" /> -->
    <link rel="stylesheet"
        href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" />


</head>

<body>
    <h1 class="icon">PES Post</h1>
    <br>
    <div class="Container">
        <!-- dispaly content on screen -->
        <% for(let post of posts) {%>
            <div class="card">
                <!-- dispaly username -->
                <h3 class="container username">@ <%= post.username %>
                </h3>

                <!-- dispaly image -->
                <% if (post.image){ %>
                    <img src="<%= post.image %>" alt="" class="image ">
                    <%}%>

                        <!-- dispaly caption -->
                        <p class="caption">
                            <%= post.caption %>
                        </p>
                        <br>
                        <!-- this syntax is wrong we should not use ':' as it is for backend (app.get("/posts/:id",(req,res))->  <a href="http://localhost:8080/posts/: <%=post.id%>">See in deatail</a>     -->
                        <div class="LSD">
                            <span class="like">
                                <!-- <span class="material-symbols-outlined">thumb_up</span> -->
                                <button class="count">
                                     <%= post.count %>
                                </button>
                            </span>
                            <!-- <button>Share</button> -->
                            <br>
                            <a href="http://localhost:8080/posts/<%=post.id%>" class="link detail">See in detail</a>
                            &nbsp
                            <a href="http://localhost:8080/posts/<%=post.id%>/edit" class="link">Edit post</a>
                            <form method="post" action="/posts/<%=post.id%>?_method=DELETE">
                                <button class="delete">Delete</button>
                            </form>
                        </div>
                        <hr id="hr">
            </div>
            <% } %>
            <a href="http://localhost:8080/posts/new" class="link newPost">Create new posts</a>
    </div>

    <script src="app.js"></script>
</body>

</html>